const config = {
  type: 'app',
  name: 'PSS Insight Indicator Sync',
  title: 'PSS Insight Indicator Sync',

  launch_path: '/index.html',
  entryPoints: {
    app: './src/App.js',
  },
};

module.exports = config;
